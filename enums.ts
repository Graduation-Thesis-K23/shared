export enum Role {
  Admin = "ADMIN",
  Employee = "EMPLOYEE",
  Manager = "MANAGER",
  Shipper = "SHIPPER",
  Branch = "BRANCH",
}

export enum OrderStatus {
  Processing = "PROCESSING",
  Approved = "APPROVED",
  Completed = "COMPLETED",
  Shipping = "SHIPPING",
}

export enum Payment {
  Cash = "CASH",
  Banking = "BANKING",
}
